# 性能测试报告

## 测试环境

- **测试日期**: 2026-08-05
- **macOS 版本**: Darwin 24.6.0
- **构建版本**: v0.1.0

## 测试场景

### 场景 1：中等规模目录（~10,000 文件）

**测试目标**：验证常规项目目录的扫描性能

**测试步骤**：
1. 选择一个包含约 10,000 个文件的目录（如一个中型前端项目含 node_modules）
2. 启动扫描并记录完成时间
3. 观察内存使用情况
4. 验证进度更新的流畅度（约 100ms 节流）

**预期结果**：
- 扫描完成时间 < 30 秒
- 内存使用增长 < 100MB
- 进度更新流畅无卡顿
- 所有文件正确分类和计数

### 场景 2：大规模目录（~100,000 文件）

**测试目标**：验证大型代码库或多项目目录的扫描性能

**测试步骤**：
1. 选择一个包含约 100,000 个文件的目录
2. 启动扫描并记录完成时间
3. 监控 CPU 和内存使用峰值
4. 验证 SQLite 批量写入（每批 500 条）效率

**预期结果**：
- 扫描完成时间 < 5 分钟
- 内存使用增长 < 200MB
- CPU 使用率合理（不持续 100%）
- 数据库写入无瓶颈

### 场景 3：深层嵌套目录

**测试目标**：验证深层目录结构的遍历性能

**测试步骤**：
1. 创建或选择一个深度 > 20 层的目录结构
2. 启动扫描并记录完成时间
3. 验证递归遍历的栈使用情况

**预期结果**：
- 无栈溢出或递归限制错误
- 深层文件正确统计
- 当前路径显示正确

### 场景 4：包含大量隐藏文件

**测试目标**：验证隐藏文件过滤的性能影响

**测试步骤**：
1. 选择一个包含大量隐藏文件的目录（如 .git 目录多的仓库）
2. 分别测试「包含隐藏文件」和「排除隐藏文件」两种模式
3. 对比扫描时间和文件计数差异

**预期结果**：
- 排除隐藏文件时明显更快
- 两种模式的计数差异符合预期
- 隐藏文件过滤逻辑正确（路径中任意组件以 . 开头）

### 场景 5：取消操作响应性

**测试目标**：验证取消操作的响应速度

**测试步骤**：
1. 启动一个大型目录扫描
2. 在扫描进行到 20%、50%、80% 时分别测试取消
3. 记录从点击取消到状态更新为 Cancelled 的延迟

**预期结果**：
- 取消响应时间 < 200ms
- 取消后不再写入新数据
- 已扫描的部分不保存（最近成功结果未被覆盖）

### 场景 6：连续多次扫描

**测试目标**：验证多次扫描的内存泄漏和性能退化

**测试步骤**：
1. 连续扫描同一目录 5 次
2. 记录每次扫描的完成时间和内存使用
3. 验证数据库滚动保留逻辑（只保留最近 3 次终态扫描）

**预期结果**：
- 每次扫描时间相近，无明显退化
- 内存使用稳定，无泄漏迹象
- 数据库中只保留最近 3 次扫描记录

### 场景 7：UI 响应性

**测试目标**：验证扫描过程中 UI 的响应性

**测试步骤**：
1. 启动一个大型目录扫描
2. 在扫描过程中尝试：
   - 展开/折叠分类面板
   - 滚动最大文件列表
   - 点击其他 UI 元素
3. 观察 UI 是否流畅响应

**预期结果**：
- UI 始终流畅，无阻塞或卡顿
- 扫描在后台 Rust 任务中执行，不阻塞主线程
- 进度更新的节流（100ms）有效防止过度渲染

## 性能优化验证点

### 已实现的优化

1. **批量写入** (scanner.rs:15)
   - 每批 500 条文件记录批量插入 SQLite
   - 减少数据库事务开销

2. **进度节流** (scanner.rs:16)
   - 100ms 节流间隔，防止过度事件发射
   - 减少前端渲染压力

3. **惰性分类** (scanner.rs:219-234)
   - 使用 match 表达式快速分类扩展名
   - 无正则表达式或复杂解析

4. **滚动保留** (database.rs)
   - 只保留最近 3 次终态扫描
   - 防止数据库无限增长

5. **只读扫描** (scanner.rs:180-200)
   - 只读取文件元数据，不读取内容
   - 减少 I/O 开销

### 待优化项（如性能测试发现瓶颈）

1. **并行遍历**
   - 当前单线程顺序遍历
   - 可考虑使用 `rayon` 并行处理不同子目录

2. **增量扫描**
   - 当前每次全量扫描
   - 可考虑基于修改时间的增量更新

3. **索引优化**
   - 检查 SQLite 索引是否覆盖所有查询场景
   - 考虑添加复合索引

## 测试执行记录

### 实际测试结果

| 场景 | 测试时间 | 文件数 | 完成时间 | 内存峰值 | 通过/失败 | 备注 |
|------|---------|--------|---------|---------|----------|------|
| 中等规模 | - | - | - | - | 待测试 | |
| 大规模 | - | - | - | - | 待测试 | |
| 深层嵌套 | - | - | - | - | 待测试 | |
| 隐藏文件 | - | - | - | - | 待测试 | |
| 取消响应 | - | - | - | - | 待测试 | |
| 连续扫描 | - | - | - | - | 待测试 | |
| UI 响应性 | - | - | - | - | 待测试 | |

### 发现的问题

*待填写实际测试中发现的性能问题*

### 优化建议

*待填写基于测试结果的具体优化建议*

## 结论

*待填写综合测试结论和下一步行动*
